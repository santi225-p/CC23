/* Basic styling for header, main sections, and footer */
body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
}
}

/* Rest of your CSS styles... */
header {
  background-color: #55a630;
  color: white;
  text-align: center;
  padding: 20px;
}

main {
  padding: 20px;
}

footer {
  text-align: center;
  padding: 10px;
  background-color: #f2f2f2;
  position: fixed;
  bottom: 0;
  width: 100%;
}

