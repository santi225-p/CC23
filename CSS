/* Basic styling for header, main sections, and footer */
body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
}
/* Basic styling for header, main, footer */
body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
}

header {
  background-color: #55a630; /* Green color */
  color: white;
  text-align: center;
  padding: 20px;
}

/* Rest of your CSS styles... */
header {
  background-color: #55a630;
  color: white;
  text-align: center;
  padding: 20px;
}

main {
  padding: 20px;
}

footer {
  text-align: center;
  padding: 10px;
  background-color: #f2f2f2;
  position: fixed;
  bottom: 0;
  width: 100%;
}

/* Add more styles as needed */
git remote add origin <URL_CC23>
git branch -M main
git push -u origin main
